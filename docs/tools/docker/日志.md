## 清理

```bash
cat /dev/null > $(docker inspect --format='{{.LogPath}}' <容器名称或ID>)
```

## 运行时设置

```bash
docker run -d \
  --name rtmp_recorder \
  --log-driver json-file \
  --log-opt max-size=10m \
  --log-opt max-file=3 \
  -v /你的/目录:/app \
  rtmp-recorder
```
